<?php

    // $whoWroteArticle = R::findAll('aricles', "ORDER BY userID DESC");

    // for($i = 0; $i <= count($whoWroteArticle); $i++){
        // mail($usersMail[$i]->email, $obj, $body, $headers);
        // R::getAssoc('');
    // }
?>